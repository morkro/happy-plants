{
  "name": "happy-plants",
  "private": true,
  "workspaces": [
    "packages/*"
  ],
  "scripts": {
    "bootstrap:app": "lerna bootstrap --scope @happy-plants/shared --scope @happy-plants/app",
    "build:app": "lerna run --scope @happy-plants/app test:unit && lerna run --scope @happy-plants/app build",
    "init:app": "npm run bootstrap:app && npm run build:app",
    "bootstrap:landing": "lerna bootstrap --scope @happy-plants/shared --scope @happy-plants/landing",
    "build:landing": "lerna run --scope @happy-plants/landing generate",
    "init:landing": "npm run bootstrap:landing && npm run build:landing"
  },
  "devDependencies": {
    "husky": "^4.2.3",
    "lerna": "^3.20.2"
  },
  "husky": {
    "hooks": {
      "pre-commit": "lerna run --concurrency 1 --stream precommit"
    }
  }
}
